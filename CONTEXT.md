## Context / Handoff (read this first if chat context is truncated)

This repo intentionally keeps a **source-of-truth “context pack”** so work can continue even if the AI chat history gets truncated.

### ✅ Current context pack

- Read: `V4_CONTEXT.md`

### How to resume quickly

1. Read `V4_CONTEXT.md`
2. Run backend + frontend
3. Verify key endpoints:
   - `GET /api/status`
   - `GET /api/zones`
   - `GET /api/money-makers`






